-- DDL: sequences
-- Standalone sequences not owned by any specific table.
-- Table-owned sequences (roles_id_seq, audit_logs_id_seq) are defined
-- inside their own _table.sql scripts.

-- Add any shared or independent sequences here.
-- Example:
--   CREATE SEQUENCE IF NOT EXISTS global_event_seq START 1 INCREMENT 1;
--   GRANT USAGE, SELECT ON SEQUENCE global_event_seq TO :"UMS_USER";

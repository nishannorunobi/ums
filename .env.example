# Copy to .env and adjust before running Docker
SPRING_PROFILES_ACTIVE=dev

# PostgreSQL — use host.docker.internal to reach a DB running on the host machine
DB_URL=jdbc:postgresql://host.docker.internal:5432/umsdb
DB_USERNAME=ums_user
DB_PASSWORD=ums_pass

# App
SERVER_PORT=8080
